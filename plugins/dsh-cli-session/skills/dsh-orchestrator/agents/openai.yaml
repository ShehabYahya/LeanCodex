interface:
  display_name: "DSH Orchestrator"
  short_description: "Guide bounded DSH work and review the evidence"
  default_prompt: "Use $dsh-orchestrator to delegate this engineering task to DSH while you retain architecture, scope, and final review."
policy:
  allow_implicit_invocation: true
